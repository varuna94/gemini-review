이 저장소는 **공개 배포되는 Claude Code 플러그인**이다. 남이 설치해 쓰므로,
잘못되면 그 사람 기기에서 스킬이 안 깔리거나 옛 판이 조용히 계속 돈다.

## 리뷰하는 방법 — 명령을 실행하지 말 것

**파일을 읽기만 하고 판단하라.** 테스트 실행 · `git` · `python` 같은 셸 명령을 시도하지
마라. 이 리뷰는 헤드리스로 돌아 명령 권한을 물을 수 없고, 거부되면 **응답 전체가 빈
채로 끝난다**(26.09.14 실측: `no output produced — a tool required the "command"
permission … auto-denied`). 그 한 번의 시도가 리뷰 전체를 없앤다. 동작을 확인하고
싶으면 코드를 읽어 추론하고, 확신이 없으면 `questions` 에 적어라.

diff 에 없는 파일은 **지적과 직접 관련된 것만** 골라 읽어라. 저장소를 넓게 읽다가
출력 상한(10분)에 걸려 빈 응답으로 끝난 적이 있다(같은 날 실측).

## 실제로 반복된 실패

1. **조용한 실패를 '이상 없음'으로 오독했다.** 이 스킬은 exit 0 도 exit 4 도
   '지적 없음'이 아니다. 인터프리터 이름이 틀려 command not found 로 죽어도
   마찬가지다. 이 계열을 특히 의심하라.

2. **파이프라인 종료 코드를 뒤쪽 명령이 삼켰다.** `grep … | sort -u` 는 grep 이
   아무것도 못 찾아도 sort 가 0 을 내서, `&& 경고 || 정상` 판정이 항상 뒤집혔다.

3. **plugin.json 의 version 을 안 올려 옛 스킬이 계속 돌았다.** 설치 캐시가
   `plugins/cache/<마켓>/<플러그인>/<버전>/` 이라 버전이 그대로면 갱신되지 않는다.

4. **fallback 경로에 마켓플레이스 이름을 하드코딩했다.** 저장소를 옮기자
   `plugins/cache/claude-config/…` 를 찾던 코드가 새 설치본을 놓쳤다. 지금은
   `plugins/cache/*/gemini-review/…` 로 마켓 이름과 무관하게 찾는다.

5. **문자열 존재 검사 가드가 죽어 있었다.** `"proc.returncode != 0" in src` 가 다른
   함수의 같은 문자열로 통과해, 병합에서 빠진 검사를 v1.3.0 · v1.3.1 두 판 동안 못
   잡았다. 가드는 **그 함수의 동작**을 봐야 하고, 코드를 되돌리면 빨개지는지(변이)로
   증명한다.

6. **병합이 코드만 봤다.** v1.3.0 은 스크립트를 테스트로 지키며 병합했지만 SKILL.md 는
   한쪽 판으로 덮여 모델 고정 · 결과 해석 경고가 사라지고 flash 사용을 권했다.
   **문서도 배포물이다** — 불변식을 테스트로 고정한다.

7. **테스트가 코드가 계산한 경로를 지웠다.** 결과 폴더 검사의 정리 코드가 변이 아래에서
   `shutil.rmtree("/tmp")` 를 실행해 사용자 파일과 다른 세션의 폴더를 지웠다(26.09.14).
   테스트는 **자기가 만든 샌드박스 안만** 지운다. 검사 대상 코드가 틀리면 그 경로는
   어디든 될 수 있다. 같은 날 두 번째: 테스트가 아니라 **검사 대상 코드의** 중단 정리가
   변이 아래에서 임시 폴더의 조부모를 지웠다(평소 실행이면 `/`). 대상 코드가 만드는
   임시 폴더는 샌드박스 **깊숙이** 두고, 같은 프로세스면 그 삭제도 샌드박스로 묶는다.

8. **git 설정이 가드를 우회했다.** 이름 변경 감지(기본값)가 `.env` → `env.txt` 이동을 새
   이름 하나로만 보여 줘, 비밀 값이 바뀐 줄이 민감 경로 판정을 통과했다. diff 를 만드는
   git 호출은 사용자 설정과 무관하게 고정한다.

9. **폴백 모델이 판정을 냈다.** 사용자 지시("flash 계열로 바꾸지 말 것")를 코드가 어겼다 —
   주 모델 빈 응답 → flash 로 리뷰 재요청 → 그 approve 가 exit 0. 1.4.0 을 만드는 도중에도
   1.3.2 교차 리뷰가 바로 이 경로로 approve 를 냈다(26.09.15, 주 모델이 명령 실행을 시도해
   권한 거부 → 빈 응답 → flash). 판정은 주 모델만 낸다. 진단 모델이 리뷰를 요청하는 코드가
   다시 생기면 결함이다.

10. **원인이 다른 실패를 한 코드로 뭉쳤다.** 시간 초과 · 도구 권한 거부 · 진짜 빈 응답이 모두
    "빈 응답", 쿼터 소진이 "도구 오류" 로 기록돼, exit 4 가 네 번 이어진 날 원인을 짚지 못했고
    틀린 처방(모델 낮추기 · 설정 고치기)으로 이어질 뻔했다. 새 실패 경로는 `_classify_run` 에
    원인을 더하고 `_meta.calls` · mode 에 남긴다. 원인 문구는 agy 출력 표면에 기대므로 `--check`
    가 그 표면을 점검한다.

11. **cp949 에서 죽는 문자가 되풀이해 들어왔다.** help 문구의 em dash(26.08.13 에 뺐는데 1.4.0
    작업 중 새 help 문구로 재발), 테스트 파일 자신(26.09.14). `PYTHONIOENCODING` 을 명시한
    콘솔은 자동 전환이 손대지 않는다. argparse 가 찍는 help · epilog 에는 cp949 밖 문자를
    넣지 않는다(`_check_help_survives_cp949`). 화면 출력은 `_safe_print` 를 거친다.

## 이 저장소에서 특히 볼 것

- **결과 계약**: 종료 경로를 더하거나 바꾸면 `_MODE_EXIT` · `_EXIT_TABLE` · README · SKILL.md 표 ·
  `_meta` 표 · CHANGELOG 가 함께 바뀌었는가. `_meta.passed` 가 `reviewed` + exit 0 밖에서 참이
  되는 길이 생기지 않았는가. 진단 모델이 리뷰를 요청하는 길이 생기지 않았는가.
- **민감 정보**: 이 저장소는 **public** 이다. 사내 저장소 이름·서버·계정·경로가
  주석 한 줄에라도 들어가면 그대로 공개된다. 한때 `gemini_review.py` 주석에
  사내 저장소 이름이 실측 사례로 적혀 있었다.
- **매니페스트가 유효한가**: `.claude-plugin/marketplace.json` 의 `source` 경로가
  실재하는가. `plugin.json` 의 `name` 이 마켓플레이스 항목과 일치하는가.
  README 의 `<플러그인>@<마켓>` 표기가 맞는가.
- **경로를 하드코딩하지 않았는가**: 설치 경로에 버전이 들어가므로 SKILL.md 의 실행
  블록은 `${CLAUDE_SKILL_DIR}` 를 쓴다(플러그인 · 개인 · 프로젝트 스킬 공통으로 Claude
  Code 가 치환한다, 26.09.14 확인). `${CLAUDE_PLUGIN_ROOT}` 는 플러그인에서만 치환된다.
  폴백에도 마켓 이름을 박지 않는다.
- **플랫폼 차이**: 우분투에는 `python` 이 없고, Windows 기본 설치에는 `python3` 가
  없다. 한쪽에서만 도는 명령을 문서나 코드가 단정하고 있지 않은가.
- **문서가 설치를 어렵게 만들지 않는가**: 남이 쓰는 저장소다. README 가 길어져
  "설치가 복잡하다"는 인상을 주면 그 자체가 결함이다.

## 이미 검증하고 기각한 지적 (다시 올리지 말 것)

- **플러그인 설치 버전은 `plugins/*/.claude-plugin/plugin.json` 의 `version` 이
  정한다.** 루트 `.claude-plugin/marketplace.json` 의 `metadata.version` 은
  마켓플레이스 자체의 메타이지 플러그인 버전이 아니다. 둘이 달라 업데이트가
  누락된다는 지적이 나왔으나, 실측 반례가 있다 — 어느 설치본은 marketplace.json
  이 `1.0.0` 인데 plugin.json 의 `1.1.0` 으로 설치됐고 캐시 경로도
  `…/gemini-review/1.1.0/` 이었다. (2026-08-27)

- **`tests/test_gemini_review.py` 의 `_EXIT_ROW` 정규식이 README 의 `` `130` · `143` ``
  행을 못 읽는다는 지적**(두 번째 숫자 뒤 백틱 누락). 사실이 아니다 — 두 번째 그룹도
  `` `?(\d+)`? `` 이고, 실제로 그 행을 `| `130` · `143` |` 로 매칭해 130 · 143 을 읽는다.
  (2026-09-14, 폴백 모델 지적)

- **Windows 에서 `_sigterm_self` 의 `os.kill` 이 테스트 러너를 죽인다는 지적.** 부르는 두 검사
  (`_check_signal_handlers_restored` · `_check_sigterm_during_sigint_cleanup`)가 `os.name == "nt"`
  에서 먼저 건너뛴다. 헬퍼 자체에도 nt 가드가 있다. (2026-09-14)
- **LLM 응답이 JSON 배열이면 `payload[k]` 가 TypeError 로 internal_error 가 된다는 지적.**
  `_extract_json` 은 `_looks_like_review`(dict 요구)를 통과한 값만 돌려준다 — 배열은 None →
  `parse_failed`(exit 1). 결과 계약 매트릭스의 "배열 응답" 행이 고정한다. (2026-09-15)
- **Claude Code 에 범용 `Skill` 도구가 없어서 `CLAUDE.md` 의 스킬 라우팅이 배선 단절이라는
  지적.** 사실이 아니다 — `Skill` 도구는 실재하고 인자는 목록에 있는 이름(슬래시 없음)이며,
  플러그인 스킬은 `plugin:skill` 형태를 쓴다. 실측: 같은 세션에서 `Skill` 도구로 `autoplan` ·
  `office-hours` · `gemini-review:gemini-review` 를 호출했다. 리뷰어는 호스트의 도구 목록을
  관찰할 수 없으므로 이 계열(실행 환경의 배선 추측)은 추측이지 관찰이 아니다.
  ⛔ **`CLAUDE.md` · `README` 의 슬래시 표기(`/office-hours`)를 "존재하지 않는 명령어"로
  읽지 말 것** — 사용자가 입력하는 표기이고, 에이전트는 같은 스킬을 `Skill` 도구로 부른다.
  (2026-09-16)

- **PowerShell 모양 검사가 무조건 건너뛰어 변이 검사가 늘 실패한다는 지적.**
  `_check_skill_powershell_block_shape` 는 건너뜀을 내기 **전에** 검사 세 건을 낸다. 변이 검사에서
  해당 변이가 실제로 빨개졌다. (2026-09-14)
