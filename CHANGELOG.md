# CHANGELOG

판마다 **달라지는 점**만 적는다. 설치본 갱신은 `/plugin update gemini-review@gemini-review`
뒤 **새 세션**이 필요하다 — 리뷰 배너 첫 줄(`Gemini 교차 리뷰 vX.Y.Z`)로 실제로 돈 판을 본다.

## 1.7.0 — 게이트: 커밋 내용 모사 · 경로 지정 리뷰 · 대체 리뷰 기록 (2026-09-17)

1.6.x 게이트를 전역으로 켠 다른 프로젝트에서 두 가지가 보고됐다. 둘 다 실측으로 재현했다.

1. **Gemini 가 수행되지 않는 날에는 커밋할 수 없었다.** 사흘 연속 쿼터 소진 · 권한 거부 · 무응답이었고,
   그 저장소의 운영 규칙("계층 장애면 대체 리뷰")대로 리뷰를 마쳐도 결과 파일이 없어 게이트가 열리지 않았다.
2. **경로 지정 커밋(`git commit -- 경로`)과 `git add … && git commit` 한 줄이 형태만 보고 막혔다.** 두
   세션이 작업 트리를 공유할 때 남의 스테이징을 건드리지 않으려고 전자를 규칙으로 쓰고 있었다.

- **게이트가 커밋 형태를 막지 않고 담을 내용을 모사한다.** 인덱스의 임시 사본에서 앞선 `git add` · `-a` ·
  `-i` · `--only`(경로 지정)를 git 의 커밋 구현과 같은 규칙으로 적용해 diff 를 만들고 해시를 잰다. 진짜
  인덱스는 건드리지 않는다. 기준 상태(스테이징된 수정 · 안 된 수정 · 삭제 · 새 파일 · 스테이징된 삭제)에서
  열 가지 형태를 **복사한 저장소에서 실제로 커밋**하고, 모사한 diff 와 `HEAD~1..HEAD` 가 바이트까지 같음을
  검사로 고정했다. 모사하지 않는 인덱스 명령(`reset` · `rm` · `stash` …)과 대화형 커밋은 계속 막는다.
- **`--paths 경로…`** — `git commit -- 경로` 가 담을 내용(HEAD 에 그 경로의 작업 트리 내용을 얹은 것)을
  리뷰한다. 스테이징을 건드리지 않는다. 결과의 `_meta.scope.kind` 는 `paths` 다.
- **`--record-fallback --reviewer 이름 --summary 요약`** — Gemini 리뷰가 수행되지 않았을 때 대체 리뷰를
  마쳤다는 기록을 남긴다(`_meta.mode: fallback_reviewed`, exit 0, `passed` false). 같은 저장소 · 같은
  내용의 **가장 최근 Gemini 시도가 exit 4** 일 때만 남기고, 시도가 없거나 Gemini 가 판정을 냈으면 거부한다
  (exit 2). agy 를 부르지 않는다. 게이트는 이 기록을 인정하며, 운영자가 `git config
  gemini-review.gateFallback false` 로 끌 수 있다. `--stats` 가 기록 수를 보고한다.
- 게이트의 설정 보호를 `gemini-review.*` 전체로 넓혔다. Claude 가 켤 수 있는 것은 `gemini-review.gate true`
  하나뿐이다. 키는 옵션 값을 건너뛴 첫 위치 인자로 읽는다 — `git config --file gemini-review.conf …` 같은
  무관한 명령을 막지 않는다(교차 리뷰가 잡았다).
- 경로 지정 모사는 파일 목록을 **표준 입력**으로 넘긴다. 명령줄로 펼치면 `git commit -- 폴더` 가 인자 길이
  상한에 걸린다(Windows 는 32,767자, 실측 재현 · 교차 리뷰가 잡았다).
- 종료 코드 집합은 그대로다. 0 · 2 · 8 의 설명에 새 옵션을 더했다.

## 1.6.1 — 게이트 검사의 Windows 경로 (2026-09-17)

**1.6.0 은 Windows CI 가 빨갛다.** 게이트 결함이 아니라 **검사**가 틀렸다. 설치되는 플러그인 코드는
1.6.0 과 같다.

- 대상 저장소 판정 검사가 Windows 임시 경로(`C:\Users\…`)를 명령에 **따옴표 없이** 넣었다. bash 규칙대로
  백슬래시가 이스케이프로 사라져 경로가 깨졌고, 게이트는 없는 폴더를 짚어 통과시켰다. 실제 bash 도 같은
  명령을 같게 읽으므로(그 커밋은 git 자신이 실패한다) 게이트는 맞게 동작했다. 경로를 따옴표로 감쌌다.
  리눅스에서는 경로에 백슬래시가 없어 보이지 않았다.

## 1.6.0 — 커밋 게이트 hook (2026-09-16)

판정 · 종료 코드 · 결과 JSON 은 1.5.1 그대로다. 플러그인에 hook 이 더해진다. 새 hook 은
`/plugin update` 뒤 **새 세션**에서 걸린다.

- **커밋 게이트(선택, 기본 꺼짐).** `git config gemini-review.gate true` 인 저장소에서, 스테이징과
  같은 내용으로 통과한 리뷰가 없으면 Claude 의 `git commit` 을 막는다. 지금까지는 커밋 전 리뷰가
  모델이 지시를 기억하는 데 기댔다. 판정은 1.5.0 이 남긴 `_meta.diff_sha256` 을 리뷰와 **같은
  함수**로 다시 계산해 대조한다. 진짜 저장소를 진짜 `--staged` 리뷰로 돌린 결과를 게이트가 읽는
  검사로 고정했다 — 해시가 한 바이트라도 갈리면 모든 커밋이 막히기 때문이다.
- 같은 해시의 결과가 여럿이면 **가장 최근 것**이 판정한다. 통과 뒤 같은 내용이 request_changes 를
  받았으면 옛 통과로 커밋을 열어 주지 않는다.
- **같은 저장소의 결과만 본다.** 결과 폴더는 저장소들이 함께 쓰고, 같은 파일에 같은 변경을 하면
  저장소가 달라도 해시가 같다(실측). 해시만 대조하면 남의 판정으로 커밋이 열리거나 닫힌다(교차 리뷰가
  잡았다).
- **확인하지 못하면 막는다.** `git add … && git commit`(게이트는 add 전의 인덱스만 본다), `-a` ·
  경로 지정 커밋, 저장소를 확정할 수 없는 명령(`env -C` · `sudo -D` · `GIT_DIR` · 변수 경로), 해석하지
  못한 명령, 파이썬 없음, 게이트 내부 오류가 모두 막는 쪽이다. `env -C` 는 처음에 경로를 버려 게이트가
  엉뚱한 폴더를 봤다(교차 리뷰가 잡았다). 경로가 적힌 이동(`cd` · `git -C` · `env -C` · `sudo -D`)은 따라가
  **대상 저장소의 설정**을 보고, 확정할 수 없으면 현재 폴더와 전역 설정 가운데 엄한 쪽을 본다 — 처음엔
  늘 현재 폴더 설정만 봐서, 게이트가 꺼진 폴더에서 켜진 저장소로 커밋하면 통과했다. hook 이
  exit 2 가 아닌 코드로 죽으면 Claude Code 가 통과시키므로, 셸 진입점이 그 경우를 2 로 바꾼다.
- 커밋 메시지 heredoc(`-m "$(cat <<'EOF' … EOF)"`)은 해석한다. `shlex` 로 나누면 본문의
  따옴표가 단어 경계를 무너뜨린다.
- Claude 가 게이트를 끄거나 지우는 `git config` 명령도 막는다. 막는 문구에는 우회 방법을 적지 않는다.
- 게이트는 git 만 실행하고 agy 를 부르지 않는다(구문 트리 검사로 고정). 코드를 보내지 않는다.

## 1.5.1 — 테스트 상태 격리 (2026-09-16)

**1.5.0 은 Windows CI 가 빨갛다.** 기능 결함은 아니고 **테스트 격리**가 빠진 것이다.
설치해서 쓰는 데는 문제가 없지만, 1.5.0 대신 이 판을 쓰는 것을 권한다.

- **상태 폴더 격리를 `_state_env` 헬퍼 하나로 모았다.** 종전에는 검사마다 손으로
  `XDG_STATE_HOME` 만 덮었는데, Windows 는 `%LOCALAPPDATA%` 를 쓰므로 그쪽에서는
  **격리가 아예 없었다** — 검사들이 개발자의 진짜 상태 파일을 공유했다. 실제로 결과 계약
  매트릭스의 쿼터 행이 쓴 차단을 뒤 행이 읽어 단락시켰고 Windows 잡만 빨개졌다.
  리눅스에서는 재현되지 않는 계열이라 로컬 검사 278건은 계속 초록이었다.
- **격리가 빠진 자리가 다시 생기면 잡는 가드를 넣었다.** 구문 트리로 함수 안뿐 아니라
  **모듈 수준 대입**과 `env["XDG_STATE_HOME"] = …` **첨자 할당**까지 본다. 의도적 예외는
  `격리 예외:` 표식으로 표시하고, 여러 줄로 나뉜 호출에서도 표식을 찾도록 **호출이 걸친 전체
  구간**을 훑는다.
  ⚠ 처음에는 좁게 짜서 세 번 샜다(교차 리뷰가 전부 잡았다) — `LOCALAPPDATA` 가 함께 있으면
  통과시켜 우회가 가능했고 예외 검사가 도달 횟수 0 인 죽은 코드였으며, 함수 안만 봐서 모듈
  수준과 첨자 할당을 놓쳤고, 호출 첫 줄 근처만 봐서 여러 줄 호출에서 오탐이 났다.
  **여덟 갈래를 각각 만들어 보고 전부 빨개지는 것을 확인했다.**
- **스위트 전체 상태 샌드박스를 걸었다 — 이것이 실제 보호막이다.** 테스트 러너가 검사보다 먼저
  `XDG_STATE_HOME` · `LOCALAPPDATA` 를 한 샌드박스로 돌린다. 검사가 격리를 아예 잊어도 진짜 사용자
  폴더에는 쓸 수 없다 — 실측으로 확인했다(매트릭스의 격리를 지워도 정적 가드는 못 봤고, 진짜 폴더는
  무사했다). 위의 정적 가드는 흔한 실수를 일찍 알리는 **보조 장치**로 내렸다. 구문 검사의 완전성을
  넓혀 달라는 교차 리뷰 지적이 회차마다 새 우회를 찾아 루프가 됐기 때문이다.
- ⚠ **정정:** 이 판의 중간 초안은 "검사 실패 문구에 em dash 가 있으면 cp949 콘솔에서 크래시해 원인이
  안 보인다"며 전용 가드를 넣었다. **실측으로 거짓이었다.** 러너가 시작할 때 `_make_stdout_safe()` 로
  stdout 을 `errors="replace"` 로 바꿔 두어, em dash 는 `?` 로 찍히고 스위트는 끝까지 돈다
  (cp949 콘솔 실측: `UnicodeEncodeError` 0줄). 그 가드는 뺐다. 실패 문구 18줄의 em dash 를 쌍점으로
  바꾼 것은 cp949 콘솔에서 읽기 쉽게 하려는 것으로만 남긴다.
  ⚠ **정정의 정정:** 처음 뺄 때 편집이 문자열 위치를 거꾸로 잡아, 함수는 남고 등록만 빠진 **죽은 검사**가
  됐고 상수 블록이 두 번 들어갔다(교차 리뷰가 잡았다). 구문 트리의 줄 범위로 다시 지웠다.
- **정의만 있고 실행 목록에 없는 검사 · 모듈 수준 이름의 중복 정의를 잡는 가드를 넣었다.** 위 사고가
  스위트를 초록으로 통과했기 때문이다 — 파이썬은 중복 정의를 허용하고, 등록 안 된 검사는 조용히 안 돈다.
- **첨자 할당 검사를 Python 3.8 에서도 돌게 했다.** 3.8 은 첨자를 `ast.Index` 로 한 겹 싸서
  `node.slice.value` 가 문자열이 아니라 노드였다 — 선언한 지원 범위(3.7+)에서 그 분기가 조용히 죽었다.
  CI 는 3.9 부터라 안 보였다. 하위 트리에서 문자열 상수를 찾도록 바꿔 버전과 무관하게 했다.
  실제 3.7.17 · 3.8.20 컨테이너에서 첨자 우회를 심은 사본이 둘 다 적발되는 것을 확인했다.
- **격리 가드가 Python 3.7 에서 스위트를 무조건 죽이던 결함을 고쳤다.** 헬퍼 구간을 `end_lineno`
  로 쟀는데 그 속성은 3.8 에 생겼다. 3.7 에서는 구간이 `def` 한 줄로 줄어 **헬퍼 자신**의
  `dict(..., XDG_STATE_HOME=root, ...)` 가 적발됐다(교차 리뷰가 잡았다). 줄 번호가 아니라 헬퍼
  하위 트리의 **노드 자체**를 빼도록 바꿨다. 실제 3.7.17 컨테이너에서 수정 전은 빨갛고 수정 뒤는
  통과한다. CI 에는 3.7 이 없으므로 모든 노드에서 `end_lineno` 를 지운 트리로 같은 판정을 내는지
  보는 검사와, 그 의존을 되살리는 변이를 함께 넣었다.

## 1.5.0 — 측정과 쿼터 단락 (2026-09-16)

리뷰가 **왜 여러 번 도는지** 가릴 수 있게 결과에 측정값을 남기고, 쿼터에 걸린 뒤의 낭비를
없앤다. 판정 · 종료 코드 · `_meta.passed` 의 의미는 1.4.0 그대로다 — `_meta` 에 필드를
**더하기만** 한다.

### 고침

- **하드 상한에 걸린 쿼터가 `timeout` 으로 뭉치던 결함(C1).** 쿼터에 걸린 agy 는 내부에서
  8회까지 재시도한다. 실측 609초였고 기본 하드 상한은 720초다 — 여유가 111초뿐이라
  조금만 더 끌면 `TimeoutExpired` 갈래로 떨어졌고, 그 갈래가 부분 출력을 버려 **쿼터라는
  사실이 기록되지 않았다.** 다음 실행이 같은 720초를 또 태웠다. 이제 부분 출력을 보존하고
  하드 상한을 판정하기 **전에** 쿼터 문구를 본다.
- **detached HEAD 에서 리뷰가 죽던 결함.** `git symbolic-ref` 가 rc 1 을 내면 예외가 새어
  `internal_error`(exit 1)가 됐다. rebase · bisect 중에는 리뷰가 아예 안 돌았다.
- **`internal_error` 가 `_meta.calls` 를 잃던 결함.** 토큰을 태운 회차가 집계에서 통째로
  빠졌다.

### 더함

- **`_meta.diff_sha256`** — 리뷰에 실제로 보낸 바이트의 sha256. diff 를 **바이너리로** 쓰므로
  Windows 에서도 같은 변경이 같은 해시를 낸다(텍스트 모드는 CRLF 로 바꿔 기기마다 달라졌다).
  ⚠ 해시 대상은 `git diff` 의 원본 바이트가 아니라 **스크립트가 재인코딩한 UTF-8** 이다 —
  `git diff | sha256sum` 과는 맞지 않는다.
- **`_meta.scope.repo` · `_meta.scope.branch`** — 커밋 단위로 회차를 묶는 루프 키.
  detached HEAD 면 `branch` 는 null 이다.
  ⚠ `repo` 는 저장소 **절대 경로**다. `--out` 으로 저장소 안에 쓰면 경로가 커밋될 수 있다.
- **`_meta.written_at`** — 결과를 쓴 시각(UTC). `in_progress` 기록에도 붙는다.
- **호출별 사용량** — 구조화 호출과 `--check` 의 agy 호출에 `usage` · `agy_turns` ·
  `agy_duration_seconds` 를 남긴다. 텍스트 호출은 래퍼가 없어 `usage_unavailable` 이다.
  수치가 아닌 값은 걸러내고 그 키 이름을 `usage_dropped_keys` 에 적는다.
- **쿼터 단락** — 차단이 기록된 뒤에는 `agy -p "/quota"` 로 먼저 확인한다(토큰 0 · 실측 6초).
  아직 차단이면 리뷰 요청 없이 exit 4 로 끝내 **609초를 다시 태우지 않는다.** 풀렸으면
  기록을 지우고 정상 진행한다. 차단 기록이 없는 평상시에는 조회하지 않는다(추가 비용 0초).
- **`--stats`** — 쌓인 결과 파일을 집계한다. agy 를 부르지 않는다.
- **`--ignore-quota-cache`** — 사용자가 강제로 단락을 무시한다.
  ⛔ Claude 가 스스로 붙이지 말 것(`--allow-sensitive` 와 같은 자기 우회 계열).

### 주의

- **exit 4 의 뜻이 둘로 갈린다.** `_meta.quota_cached: true` 면 캐시 단락이다 — 즉시
  재시도해도 같은 코드가 나온다. `retry_after` 뒤에 돌릴 것.

## 1.4.0 — 결과 계약

종료 코드 · 결과 JSON · 플래그의 뜻이 바뀐다. **자동화가 결과를 읽는다면 아래를 먼저 볼 것.**

### 달라지는 점

| 무엇 | 1.3.x | 1.4.0 |
|---|---|---|
| 주 모델이 빈 응답일 때 | 폴백 모델(flash)로 **리뷰를 다시 요청**, 그 approve 가 exit 0 | **판정은 주 모델만.** 진단 모델은 한 줄 생존 확인에만 쓴다 |
| 주 모델만 무응답 · 진단 모델은 응답 | 폴백 모델의 판정 | exit 4 · `primary_model_unavailable` |
| 생존 확인 뒤 재시도 | 폴백 모델 구조화 → 텍스트 | 주 모델 텍스트 재시도 한 번(같은 모델 구조화 재시도 없음) |
| agy 출력 시간 초과 · 래퍼 하드 상한 | "빈 응답" → 생존 확인 · 재시도 · 하드 상한은 exit 2 | exit 4 · `timeout`(`_meta.timeout_kind: agy \| hard`), **재시도 없음** |
| 구독 사용량 한도(쿼터) | exit 2 · `tool_error` | exit 4 · `quota_exhausted` · `retry_after` |
| agy 가 모델명을 모름 | exit 2 · `tool_error` | exit 2 · `model_unavailable` |
| `--staged` 인데 스테이징이 비었음 | exit 0 · `no_changes` | **exit 8** · `no_changes` (`--allow-empty` 면 0) |
| 스크립트 내부 예외 | traceback · exit 1 · 파일은 `in_progress` | exit 1 · `internal_error` 기록(traceback 은 그대로) |
| 결과 JSON | 최상위 `mode` · `model` · `elapsed_seconds` | **`_meta`** 를 모든 종료 경로가 남긴다(아래) |
| LLM 응답의 `model` · `_meta` 같은 키 | 결과 파일에 섞일 수 있었다 | 스키마 키(`verdict` · `summary` · `findings` · `questions`)만 옮기고 나머지는 버린다 |

`_meta` 필드: `mode` · `exit_code` · **`passed`**(주 모델이 리뷰해 통과 판정일 때만 true) ·
`model` · `elapsed_seconds` · `calls`(agy 호출마다 단계 · 모델 · **원인** · 소요) ·
`scope`(범위와 **해석된 SHA**) · `plugin_version`, 경우에 따라 `timeout_kind` ·
`dropped_llm_keys`.

### 플래그

- `--fallback-model` → **`--probe-model`**, `--no-fallback` → **`--no-probe-fallback`**. 뜻이
  "판정도 내는 대체 모델" 에서 "진단 전용 모델" 로 바뀌어 이름을 바꿨다. 옛 이름은 1.4.x
  동안 **경고와 함께** 같은 뜻으로 받는다(2.0 에서 뺀다).
- `--allow-empty` 신설 — 빈 스테이징을 8 대신 0 으로(그래도 `_meta.passed` 는 false).
- `--check` 신설 — 코드를 보내지 않고 python · git · agy · 로그인 · 모델 응답과, 해석 계층이
  기대는 agy 출력 표면(래퍼 필드 · 시간 초과 안내 · 모델 없음 안내)을 점검한다.
- `--help` 에 모든 플래그의 형식 · 기본값과 종료 코드 표를 싣는다.

### 호환

- 0 · 1 · 2 · 3 · 5 · 6 · 130 · 143 의 뜻은 그대로다. **4 의 원인이 여럿으로 갈렸고**, 8 이 새로 생겼다.
- 결과 JSON 최상위 `mode` 는 모든 결과에, `model` 은 **인자 해석을 마친 뒤의 모든 결과**(중단 ·
  내부 오류 포함)에 1.4.x 동안 `_meta` 와 같은 값으로 함께 쓴다(2.0 에서 뺀다). `model` 은 언제나 주 모델이다 — 1.3.x 의 `text_fallback` 은 진단 · 폴백
  모델을 적을 수 있었다.
- 1.3.x 의 다른 최상위 진단 키는 옮겼다: `elapsed_seconds` → `_meta.elapsed_seconds`(전체 소요),
  `probe_seconds` · `fallback_probe_seconds` · `text_model` → `_meta.calls[]`(호출마다 단계 · 모델 ·
  소요), `probed_models` 는 그대로다.
- 최악 소요(`--timeout 10m`)는 약 27분이다(구조화 720 + 생존 확인 180 + 텍스트 720초).
  1.3.x 는 폴백 구조화 720초가 더 붙었다.

### 안에서 바뀐 것

- agy 호출이 `_run_agy` 한 곳으로 모였다(`--mode plan` · 하드 상한 · stdin 차단).
- 원인 판정이 `_classify_run` 한 곳으로 모였다(구조화 · 생존 확인 · 텍스트 재시도가 같은 규칙).
- 테스트: 결과 계약표의 모든 행을 가짜 agy 로 도는 매트릭스, 실측 출력으로 원인 분류,
  cp949 도움말, 해석된 SHA, `--check`. 변이 검사 41개가 전부 기대한 이유로 빨개지는지 판마다 본다.
- CI(GitHub Actions): ubuntu · windows 에서 스위트, ubuntu 에서 변이 검사.

## 1.3.2 — 병합에서 사라진 가드 복구 (2026-09-14)

동작 계약(종료 코드의 뜻 · `--out` 키)은 그대로인 결함 수정이다.

- 텍스트 재시도가 agy 실패 · 출력 시간 초과의 부분 출력을 리뷰 원문으로 쓰던 길을 막았다(v1.3.0 병합에서 사라졌던 가드).
- `--out` 을 인자 해석 **전에** 무효화하고, 쓸 수 없으면 리뷰를 시작하지 않는다(exit 2). 결과 파일은 원자적 · 사용자 전용(0600, `$XDG_STATE_HOME/gemini-review/`).
- 중단(SIGTERM · SIGINT · SIGHUP) 때 임시 폴더를 정리하고 `mode: interrupted` · exit 130 · 143 을 남긴다.
- agy 를 절대 경로로만 실행한다(현재 폴더 · 저장소 안 PATH 항목 제외). agy 가 실패를 알리면(rc≠0 · `status=ERROR`) 빈 응답으로 읽지 않는다.
- diff 를 사용자 git 설정과 무관하게 만든다(`diff.renames=false` 등 — 이름 변경으로 민감 경로 판정을 비껴가던 길).
- 판정 모델을 코드가 대입한다(LLM 응답의 `model` 이 남던 결함). 리뷰 JSON 이 둘 이상이면 판정하지 않는다.
- SKILL.md: flash 권장 삭제, 모델 고정 절 복구, `${CLAUDE_SKILL_DIR}` + 인터프리터 자동 탐지 블록, 백그라운드 실행 · 파이프 금지, `--allow-sensitive` 자동 사용 금지.
- 배너 · `--version` 에 판과 스크립트 경로. 변이 검사(`tests/mutation_check.py`).
- 리뷰 프롬프트가 셸 명령 시도를 막는다(헤드리스 agy 가 권한을 자동 거부하면 응답 전체가 사라졌다).

## 1.3.1 (2026-09-14)

- SKILL.md 실행 경로를 `${CLAUDE_PLUGIN_ROOT}` 로 — 플러그인 설치본에서 안내된 경로에 파일이 없었다.

## 1.3.0 (2026-09-11)

- 두 PC 에서 갈라진 판을 병합했다(종료 코드 5 · 6, 민감 경로 가드 수정, 생존 확인 · 폴백).
  ⚠ 이 병합에서 텍스트 재시도 가드와 SKILL.md 의 모델 고정 절이 사라졌다 — 1.3.2 에서 복구.

## 1.2.0 이전 (2026-08-27)

- 독립 플러그인 저장소로 분리. 민감 경로 판정 · 결과 파일 무효화 · 빈 응답 진단의 초기 판.
