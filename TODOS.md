# TODOS

미룬 항목과 다음 판 계획. 계획 문서는 커밋하지 않고, 남는 일만 여기에 둔다.
판마다 달라진 점은 `CHANGELOG.md` 에 있다.

## 다음 큰 판 — 2.0 (1.4.x 호환을 걷어낸다)

- 결과 JSON 최상위 `mode` · `model` 제거(`_meta` 만 남긴다).
- 옛 플래그 `--fallback-model` · `--no-fallback` 제거.
- **1.5.0 이 더한 `_meta` 필드의 처분** — 늘어난 필드마다 커밋 게이트 hook · `--stats` ·
  외부 자동화가 묶인다. 2.0 에서 한 번에 정리한다.
  - **유지**: `diff_sha256`(hook 의 선결 조건) · `scope.repo` · `scope.branch` ·
    `written_at` · `quota_cached`
  - **재검토**: `diff_bytes`(루프 종류 판별에만 쓴다 — `--stats` 가 다른 손잡이를 찾으면 뺀다) ·
    `calls[].agy_turns` · `calls[].agy_duration_seconds`(`seconds` 와 겹친다) ·
    `quota_cached_streak` · `quota_source` · `quota_buckets`
  - **조건부 제거**: `usage_dropped_keys`(agy 가 늘 수치만 주는 것이 확인되면)

## 미룬 항목

### 1.6.0 후보 — 1.5.0 에서 뺀 것
- **`--stats-out <경로>`** — 집계를 JSON 으로 쓴다(`--out` 재사용 금지: 인자 해석 전에
  무효화되기 때문). 0건은 `{"reviews": 0}`. 뺀 이유는 인자 계약을 하나 더 늘리는데
  기준선 확정은 사람이 읽는 출력으로 달성되기 때문이다.
- **`--help` 그룹 재편**(모드 · 범위 · 실행/모델 · 우회) — cp949 가드가 걸린 표면이라
  이득 대비 검증 비용이 크다.
- **리뷰 스키마에 "리뷰어가 연 파일 수"** — 1.4.0 계약 불변을 벗어나고, 스키마·프롬프트
  표면은 빈 응답률과 직결된 이력이 있다(26.09.11 · 09-14 실측). 재개 조건은 빈 응답 비율
  기준선 확보와 롤백 트리거 명시다.
  ⚠ 원래 근거는 "입력 토큰이 안 보일 때의 유일한 대리 지표" 였는데, agy 1.2.3 이
  `input_tokens` 를 낸다는 것이 확인되어(26.09.16) **그 근거가 사라졌다.**
- **프롬프트에 "실행 환경을 단정하지 말라" 한 줄** — 리뷰어가 diff 밖의 호스트 도구 목록 ·
  셸 · 권한 문법 · 외부 저장소 상태를 단정해 거짓 지적을 내는 계열이 있다(26.09.16 하루에
  연속 4건, 전부 기각). 대상이 에이전트 설정·문서일 때 특히 잦다. 프롬프트 표면 변경은
  빈 응답률과 직결되므로 **`--stats` 가 기각률을 재기 시작한 뒤에** 넣는다.

### 커밋 게이트 후속 — 1.6.0 · 1.7.0 에서 뺀 것
- **split index(`core.splitIndex`)** — 커밋 모사가 인덱스 파일만 임시로 복사하므로, 공유 인덱스 파일을
  따로 두는 저장소에서는 git 이 사본을 읽지 못해 게이트가 막는다(fail-closed). 겪으면 `write-tree` →
  `read-tree` 로 사본을 만드는 쪽을 본다(대신 stat 캐시를 잃어 `-a` 가 느려진다).
- **clean 필터(LFS 등)가 두 번 돈다** — 모사한 `git add` 가 필터를 실행하고 실제 명령이 한 번 더 실행한다.
  큰 파일이 많은 저장소에서 게이트 시간이 길어지면 본다.
- **Windows 실측** — Git Bash 에서 셸 진입점을 실제 hook 으로 돌려 본다. CI 는 게이트 모듈과
  POSIX 셸 진입점만 본다(Windows 의 `bash` 는 러너에 따라 WSL 을 가리킬 수 있다).
- **git alias** — `git ci` 는 빠른 경로의 `commit` 낱말 거르기를 비껴간다. 막으려면 Bash 호출마다
  alias 를 풀어야 해 비용이 크다.
- **`!` 명령 모드가 hook 을 거치는지** 실측 — 사람이 게이트를 켠 채 Claude Code 안에서 직접
  커밋할 수 있는지가 여기에 달렸다.
- `--out` 으로 다른 곳에 쓴 결과를 게이트가 볼지.

### 실제 Windows 에서 확인 (cp949 콘솔 · PowerShell 실행 블록)
- **What:** Windows 기본 콘솔에서 `python tests/test_gemini_review.py` 와 실제 리뷰 한 번.
  SKILL.md 의 PowerShell 실행 블록을 실제로 돌려 인터프리터 탐지 · 못 찾으면 exit 2 ·
  `exit $LASTEXITCODE` 전달을 본다(지금 테스트는 그 블록의 모양만 본다).
- **Why:** cp949 는 `PYTHONIOENCODING` 흉내로만 확인했다. v1.3.1 에서 테스트 자신이 cp949
  에서 죽던 이력이 있고, 1.4.0 작업 중에도 help 문구의 em dash 가 cp949 에서 `--help` 를
  죽였다. CI 의 Windows 러너는 한국어 로캘이 아니라 대신하지 못한다.

### agy 하위 프로세스 그룹 종료
- **What:** 하드 상한 · 신호 종료 때 agy 의 손자 프로세스까지 정리(POSIX 는 새 세션 +
  그룹 종료, Windows 는 `CREATE_NEW_PROCESS_GROUP`).
- **Why:** `subprocess.run` 은 직접 자식만 죽인다.
- **Cons:** 플랫폼 분기. 현재 agy 는 한 줄 프롬프트에서 하위 프로세스를 띄우지 않는다(실측).
  `--check` 는 아직 하위 프로세스 수를 재지 않는다 — 재게 되면 보이는 순간 우선순위를 올린다.

### 백그라운드 실행이 호스트에 끊길 때
- **What:** Claude Code 의 백그라운드 Bash 작업이 가용 메모리가 충분한데도 "system is running
  low on memory" 로 리뷰를 끊은 일이 있다(26.09.14, 가용 13GB · agy 230MB). 그때 오는 신호가
  SIGTERM 인지 SIGKILL 인지 재고, SIGKILL 이면 남는 임시 폴더를 다음 실행이 치우는 방안을 본다.
- **Why:** SKILL.md 절차가 백그라운드 실행을 권한다. SIGKILL 은 신호 정리로도 못 잡는다
  (`_meta.mode` 가 `in_progress` 로 남아 통과로 읽히지 않는 것이 유일한 방어).

### 기타
- 같은 `--out` 경로로 동시에 돌릴 때의 경고를 문서화.
- README 에 실제 출력 예시.
- 새 판 알림.
- 생존 확인에서 주 모델 · 진단 모델을 동시에 찌르기(최악 대기 180초 절약).
- agy 경로를 환경 변수로 지정 — 저장소 설정으로 심을 수 있는 위험을 풀 방법이 생기면.
- `--staged` 와 `--base` · `--two-dot` 을 함께 주면 거부.
- LICENSE 추가(소유자 결정).
